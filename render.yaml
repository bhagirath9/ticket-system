services:
  - type: web
    name: ticket-system
    runtime: go
    buildCommand: go build -o server ./cmd/main.go
    startCommand: ./server
    envVars:
      - key: PORT
        value: 8080
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        value: tickets.db
